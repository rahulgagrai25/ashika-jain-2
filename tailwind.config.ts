import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        tan: ['Tan', 'sans-serif'],
        evolve: ['Evolve', 'sans-serif'],
      },
    },
  },
};

export default config;

